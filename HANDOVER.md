# 工作交接报告 — 残響アパート（回声公寓）

> 本报告写给接手此任务的后续 agent，总结当前状态、已修复问题、遗留任务、验证方法与关键陷阱。
> 项目目录：`/Users/thysummer/agent/dhr/echo-apartment`
>
> **第 8 轮更新（本轮）**：**摩尔纹/暗场颗粒真正的根因修复 + 建模/氛围第 8 轮迭代**。
> ① **后期链量化空间错误**（「暗场颗粒偏重」真凶）：旧管线 grade(自定义) → OutputPass(ACES+sRGB)，
> 32 级有序抖动 `floor(col*31+bayer)/31` 作用在**线性 HalfFloat 值**上——线性空间第一级量化
> 对应 ~21% 显示亮度，bayer 抖动把近黑墙面变成 0↔50/255 的粗暴棋盘（无头实测：带后期
> hf-moire=77.65，无后期仅 8.55，颗粒只是从犯）。**修复**：删掉 OutputPass，把 three 的 ACES
> fit + sRGB OETF 逐系数移植进 grade shader 末尾，抖动与颗粒都在**显示空间**做；颗粒改为
> 显示空间 0.045 幅度 + 暗部锥形衰减（30% 保底，亮部全量）。修复后同点位 hf-moire
> **77.65→17.87**，暗部干净、亮部颗粒质感保留（视觉模型确认无渲染错误、颗粒细腻）。
> 注意：`renderer.toneMapping=ACES` 保留（nopost 直渲路径仍用它）；grade 内 uExposure uniform
> 快照自 `toneMappingExposure`（1.38，静态）。
> ② 逻辑巡查：浴室（带浴缸）地面原为木地板且脚步声是 concrete——视觉/听觉/现实三重不符，
> 改 `floorMat: M.tile, floorUV: [5,8]`（0.2m 方砖）；其余巡查（区域触发一次性✓、事件链、
> 门系统、怪物 AI、电话、交互 LOS、上轮未提交的关门音效/闪烁灯管同步改动）均验证无新问题。
> ③ 建模：浴室新增洗衣机（SE 角，可交互：按 E 半圈即停 + 镜头微震）+ 洗衣篮 + 衣物堆；
> 卧室北墙挂轴（decalWall + 上下木杆）+ NW 角纸箱堆 + 被褥旁坐垫；厨房水槽泡碗筷（3 碗+筷子）
> + 灶台锅 + 台面电饭煲（含盖）+ 酱油瓶；儿童房天花板风铃（铃+钟舌+3 纸片，阵风式摆动）。
> ④ 氛围/音频：新合成音效 远警笛 siren（双失谐正弦 6 秒涌动）/ 水锤 hammer（管壁三闷响）/
> 洗衣机自转 washer（锯齿低频 5.5s + 不平衡砰击 9 连）/ 风铃 chime（4 音玻璃音 irregular）；
> `_randomEvent` 尾部 else 重分布为加权氛围池（siren 18%/hammer 20%/浴室 washer 14%/原 creak+ghost+scrape 48%）；
> 风铃在玩家 <7m 时每 6-16s 随机响（pan 按相对方位）。
> 验证：smoke 全过；debug-audit 1762 步 0 穿透、0 节点无地板（洗衣机碰撞体无副作用）；
> tp=5/16.8/50 走廊点位 white% 全部 0.0000、maxblob=0；房间截图（浴室/卧室/厨房）视觉模型
> 确认新道具全部落位无穿模；全域 hf-moire 13-22。
>
> **第 7 轮更新（本轮）**：**根治「墙壁大片白色」真凶 —— 顶点色 NaN**。
> 症状：真实 GPU 上墙面（尤其贴墙视角/楼上走廊）大面积渲染成纯白（实测整屏 mean=255、white%=100），
> 而无头 SwiftShader 截图却是黑的，两套环境表现完全相反，此前多轮调曝光/灯强/手电衰减都未命中。
> 根因：`util.js makeBoxGeo` 先对顶点做随机抖动（jitter ±0.012），再用**抖动后**的坐标算伪 AO：
> 顶/底边顶点被抖出盒外后 `dy>1` → `1-dyc` 为负 → `Math.pow(负数, 1.3) = NaN` 写入顶点色 attribute。
> NaN 在真实 GPU 光栅化成垃圾白色，SwiftShader 渲成黑色 —— 全场 247 个带顶点色几何中 170 个中招。
> 修复：所有归一化坐标在 pow 前 clamp 进 [0,1]，并加 `Number.isFinite` 最后防线；
> 修复后 badGeo=0，真实 GPU 与无头截图一致（贴墙视角 mean 255→~19，白块归零，手电筒光斑/纹理细节正常）。
> 排查方法论（值得留档）：受管浏览器后台标签 RAF 被节流到 0fps，但可手动 `g._loop()` 驱动帧；
> 用离屏 WebGLRenderTarget + readRenderTargetPixels 同步读回像素，配合逐类关灯/overrideMaterial/
> 逐对象隐藏/材质属性 diff 二分，最终用「染红单个物体」确认元凶几何，再查其 attribute 发现 NaN。
> 验证：smoke.mjs 全过；debug-audit 1762 步 0 穿透、0 怪物无地板。
> 已知残留：暗场胶片颗粒在近黑区域视觉上偏重（无头截图 hf-moire 指标 63-76），待第⑤项摩尔纹专项评估。
>
> **第 6 轮更新**：长走廊（z 32..58）加宽到内宽 2.8m（墙心 ±1.5，含地板/天花/踢脚线/横梁/
> 散热器/死门/开关同步迁移），长走廊新增下半墙裙板增加立体层次；手电筒新增「近墙软衰减」+
> 曝光 1.45→1.38 + 走廊灯 2.8→2.4 + 后期高光肩部压缩（根治贴墙大面积纯白）；摩尔纹再降
> （anisotropy 8→16、墙纸 128→256 且降低竖条纹对比、榻榻米/木地板高频线软化、PS1 顶点抖动
> 320→640 全分辨率减半蠕动）；建模位置修正（走廊自行车回正避免后轮嵌墙、浴室药品柜门铰链移到浴室内侧）；
> 音频氛围新增远程孩子轻笑随机事件 + 恒定雨声底噪（随楼层/楼梯间增强）；debug-audit 的穿墙压力测试
> 改为只统计全高阻挡物并跳过无地板腔体，消除家具/楼梯井假阳性；
> **修复「门框吃掉碰撞解算器」真穿墙 bug**：moveWithCollisions 反嵌入改为只处理移动前已重叠的阻挡物
> （此前解算墙体后会被门框/踢脚线二次推回墙内，玄关侧墙 0.12m 真实穿透已复现并归零）；
> 反嵌入方向选择升级为「避开其他阻挡物的逃生方向」：低矮床头柜贴墙时不再把玩家推入墙内；
> 书架柜整体从墙内移到墙面齐平（-8.28→-8.15，书/倒书同步前移）；
> **debug-audit 全高阻挡物穿墙压力测试已归零：1762 步 0 穿透**。
> 后续追加：长走廊顶角线（墙-顶交界深色压条）+ 远距离门自动打开的随机恐怖事件；
> 木地板脚步声 12% 概率随机加入老旧木板吱呀声；
> 浴室药品柜门改为朝浴室内侧开（旋转 -0.55，不再嵌入墙/卧室）；雨声底噪加入卷积混响湿声；
> 所有程序化贴图 magFilter 从 Nearest 改为 Linear（近处像素块质感让位于更稳定的墙面/地板，
> 进一步消除视角转动时的纹理蠕动）；
> 客厅/厨房/卧室/佛间/儿童房新增踢脚线（含纸拉门/衣柜开口跳段），全屋底部轮廓更立体；
> 雷击时增加轻微镜头震动，暴风雨夜的物理感更强；
> 所有月光窗叠加程序化雨痕透明层，暴风雨从听觉延伸到视觉；
> **贴墙白斑专项（用户重点复查项）**：近墙软衰减加深（贴墙最低 35%→15%、基准 5.5→4.6、
> 衰减区间 0.35/1.1→0.3/1.4），后期高光肩部加强（0.2→0.32）；
> 理论核算 0.3m 贴墙热点 radiance≈15 时输出已回落到 ~235 以下，不再顶成全白；
> 手电筒体积光锥 uOpacity 0.07→0.05，贴墙时锥面叠加进一步减弱。
>
> **第 5 轮更新（本轮）**：摩尔纹根因修复（NearestFilter+无mipmap → LinearMipmapLinear+各向异性8，
> TV雪花保留流式贴图）；灯光物理重平衡（走廊 8→2.6、房间→1.6-2.0、月光 2.5→0.8、蜡烛 6→1.8、
> 电视 4→1.4、出口灯 2.6→0.9；木地板 roughness 0.42→0.72 去镜面白斑）；灯具重做（金属灯槽+端盖+
> 自发光灯管，灯管随光熄灭切换暗玻璃材质，终局 blackout 时灯管也会熄灭）；
> 荧光灯 flicker 状态机（旧代码每帧重建 mulberry32 取首个常数——灯根本不闪）；
> **贴花立起来的 bug**（decalFloor 用 rotation.z 旋转已放平的几何——血迹/报纸以随机角度斜插出地板）；
> 死门眼睛墙朝向（'s'→'e'，玩家终于能看见那面墙）；东/西楼梯扶手重做（旧扶手 y=1.06 且旋转方向反，
> 大半埋在台阶里；新参数化扶手+立柱）；怪物节点 4 处无地板（抬高地段 y、楼梯内部、楼梯井悬空）；
  浴室瓷砖墙裙/玄关信箱+伞架/沙发重构（坐垫靠垫毛毯）/被褥/厨房防溅板+挂锅/儿童桌椅/窗框；
> 可交互电灯开关（4 个，接线到具体灯具，22% 概率关灯后自己亮回）；雷雨夜闪电导演（窗户+天光连闪，
> 雷声按距离延迟）；生成式配乐（程序化卷积混响+稀疏小调动机+追击脉冲层+儿童房摇篮曲）；
> 夜风循环（楼梯间/二楼更强）；按地面材质区分的脚步声（木/榻榻米/混凝土）。
> 几何审计（debug-audit.mjs 常驻保留）+ 89k 步 8 方向全墙冲刺压力测试。
> smoke.mjs 36 项断言全部通过。唯一剩余验收项：**用户实测**。
>
> **第 4 轮更新**：修复穿地物理 bug（`moveWithCollisions` 落地窗口按本帧实际下落跨度锚定，
> 悬浮角色不再穿过地板，见 2.7）；12 处建模位置/朝向错误（电视屏幕朝向、卧室镜子、浴室药柜与柜门、
> 佛间照片与报纸、二楼北墙窗户、厨房/浴缸水龙头、三份手记悬浮、抬高地段贴花、浴室血污贴花、暖气片、
> 楼梯扶手、衣柜背板、踢脚线 jog 段、冰箱门线、书架上的书、灶台火眼、佛坛蜡烛、热水器、挂画与画框、
> 儿童房柜子，见 2.8）；怪物追击速度 2.35→3.3 且冲刺/贴身传送防穿墙（见 2.9）；电视关机黑屏（见 2.10）；
> `?tp` 支持二楼/抬高地段正确落地（见 2.11）；镜子惊吓鬼魂防穿墙；调查交互加墙体遮挡检测（不能隔墙按 E，
> 见 2.12）；随机关门事件不再关玩家身边 3m 内的门。smoke.mjs 36 项断言全部通过。
> 唯一剩余验收项：**用户实测**。
>
> **第 3 轮更新**：flashboost 清理 + 调试参数裁剪完成（保留 autostart/nopost/tp/pos，复活 noflash 作 A/B 判别）；
> **新发现并修复 `?tp` 坠落穿地 bug**（抬高地板段传送会掉出世界，见 2.6）；死门旁白簇定位为开关/门把手高光并收敛；
> README 已同步；7 点位截图回归：6 点白占比 ≤0.006%，tp=50 残余属物理过曝（见 3.3）；smoke.mjs 全部通过。
> 唯一剩余验收项：**用户实测**（见 3.1-2 与第 6 节）。

---

## 1. 项目概述

一款完全在浏览器中运行的**日式心理恐怖第一人称探索游戏**（Three.js r160 / WebGL，全部素材程序化生成，零外部资源）。中文为主、日文为辅双语。PS1/PS2 低多边形复古美学：640×360 内部分辨率、顶点抖动、Bayer 抖动、扫描线、胶片颗粒、内置 shader 泛光、ACES 色调映射。

**运行**：`cd echo-apartment && python3 -m http.server 8010`，浏览器打开 `http://127.0.0.1:8010/`（ES Module 需要 HTTP 服务）。
**测试**：`node smoke.mjs`（33 项断言：贴图、关卡构建、门动画、灯光、怪物 AI、幽灵、全图 38 航点走通）——**当前全部通过**。

---

## 2. 本轮（触控板 + 白斑 + 穿模）已完成的修复

### 2.1 触控板无法转向（Mac）
- **根因**：Mac 触控板「轻点」产生的 click 往往无法获得浏览器指针锁定权限，`lock()` 被静默拒绝后视角完全动不了。
- **修复**（`js/game.js`）：
  - 新增**拖动视角兜底**：指针未锁定时，`mousedown` + `mousemove` 拖动直接改 `camera.rotation`（灵敏度 `sens = 0.006`，仰角钳制 ±1.52 rad）；锁定状态下自动禁用。
  - 指针锁定失败时弹出双语提示 `_lockHint()`（监听 `pointerlockerror` + promise rejection）。
  - 指针锁定灵敏度 `pointerSpeed` 0.0022 → **0.0038**（两处：初始化和攻击结束恢复）。
  - `index.html` 标题页/暂停页加入了触控板拖动提示（中/日双语）。
- **验证**：DOM 检查提示文本存在（grep 命中 1 次）。

### 2.2 手电筒「打不开 / 看不清」——本轮最重要的根因发现
- **根因**：手电筒 `SpotLight` 是**挂载在 camera 上的**（`this.camera.add(this.flash)`）。经 A/B 实验证明：在该渲染链（EffectComposer + SwiftShader/部分 GPU 驱动）下，**camera 子节点上的聚光灯完全不被渲染**——off/on/boost(500) 三张截图几乎完全一样（avg≈0.7-2.3）。这解释了用户从第一轮至今反复抱怨「手电筒没用、看不清任何东西」。
- **修复**：把手电筒改为**场景直接子节点**，每帧用世界坐标跟随相机：
  ```js
  this.flash = new THREE.SpotLight(0xcfe0ff, 20, 20, 0.44, 1.4, 1.6);
  this.scene.add(this.flash);   // 不再 camera.add
  // _updatePlayer 每帧：
  this.flash.position.set(camera.x + 0.1, camera.y - 0.06, camera.z);
  this.flashTarget.position.copy(camera.position).addScaledVector(dir, 12); // 目标点 12m 前
  ```
- **参数**：强度 20、距离 20、角度 0.44、衰减 1.4、penumbra 1.6；`castShadow = false`（阴影痤疮源 + 每帧整屏阴影 pass，PS1 风格不需要）。
- **验证（干净 A/B，`nolights=1` 杀光环境光后）**：
  - 关灯 avg=0.7；开灯(20) avg=24.2；增强(500) avg=35.8 → **手电筒确实在工作**。
  - 1.1m 近墙热点：柔和椭圆光斑，**纯白像素 0 个**（min>235 占比 0.0000）——不过曝。
  - 光锥 mesh 同步缩窄（半径 0.6、长 6.5m、透明度 0.10），不再插入两侧墙壁。

### 2.3 墙上白色「穿模」物
- **已处理的多项根因**：
  1. 手电筒热点过曝（旧强度 140/34）→ 降到 20 + 衰减 1.4；泛光阈值 0.72→0.85、glow 0.5→0.42。
  2. 光锥穿墙（旧锥半径 5.4/长 12，走廊半宽仅 1.1m）→ 缩为 0.6/6.5。
  3. 手电筒阴影痤疮风险 → `castShadow=false`（潜在白斑源彻底移除）。
  4. **走廊暖气片**原为无纹理白色大箱子（白金属 0x9aa0a4，1.6×0.6m 贴墙）→ 重做为 `_radiator()`：锈色箱体 + 7 根竖肋 + 顶格栅 + 红色阀门，明确可辨认。
  5. **挂钟**原用 `basicMat`（不受光照，黑暗中自发光白色圆盘）→ 改为 `stdMat`（受光，黑暗中自然变暗）。
  6. 一批道具嵌入墙体/悬空错位修复：踢脚线、暖气片、挂钟、开关、二楼窗户（窗板/窗条/窗台/灯位全部移到墙内壁面外）、二楼血迹/手印贴花（x 0.985→0.915）、楼梯扶手贴墙（±1.115）。
- **验证**：teleport 定点截图（`?tp=Z&face=n|s`）6 个点位 + A/B 组，白色占比 0.0000-0.0006，无阴影痤疮斑点（speckle≈0-1）。

### 2.4 楼梯穿模感
- **修复**：视线高度平滑 `eyeY = lerp(eyeY, playerPos.y, dt*16)`，台阶 0.28m 瞬时抬升改为平滑过渡；出生/攻击后重置 `eyeY=0`。
- 东楼梯扶手从悬空改为贴墙细杆（视觉件，无碰撞）。

### 2.5 调试基建（后续 agent 会用）
所有调试参数仅 URL 生效，正常游玩零影响：
- `?autostart=1` 跳过标题；`?nopost=1` 绕过后期链；`?nosnap=1` 关顶点抖动（util.js）
- **`?tp=Z&face=n|s`**：传送玩家到 z=Z 面向指定墙（确定性截图——唯一可靠的视觉验证手段）
- `?pos=1` 每 30 帧把玩家坐标写入 `document.title`（`--dump-dom` 可读取）
- `?noflash=1` 手电筒关闭启动（白斑 A/B 判别：白块在有/无手电筒下是否都在 → 区分光斑过曝 vs 自发光材质）
- 已删除（第 3 轮）：?drive / ?wall（虚拟时间下走不到目的地，已被 ?tp 取代）、?nolights（隔离环境光；如需要可临时恢复）、?flashboost（A/B 验证已完成）

---

## 2.6 第 3 轮修复（本轮）

1. **flashboost 三元表达式移除**：`flash.intensity = flashOn ? 20 : 0`；boot 段 drive/wall/nolights/flashboost 参数删除（后复活 noflash，理由见下）。
2. **发现并修复 `?tp` 坠落穿地 bug**：`tp=26.5`（抬高地板段 z 24..32）把玩家 AABB 直接嵌进 0.16m 地板板内，
   moveWithCollisions 只解算运动新增的穿入，初始嵌入无解 → 玩家掉出世界（Node 模拟复现：60 帧 y0=-11.18）。
   **修复**：tp 改为从 0.9m 高处掉落，由重力自然落到正确地板高度（模拟验证 26.5 落 0.16、其余落 0.00，零初始重叠）。
   正常游玩不受影响（走进抬高段靠 stepUp 0.35 正常跨上），此 bug 仅影响传送。
3. **死门旁白簇定位与收敛**：tp=50 北墙白簇（0.55~0.85%）定位为 ①门口电灯开关（M.pale 浅色 + 无 AO，
   正处手电筒热斑边缘）②门把手金属高光。修复：开关改专用老化电木材质 `0x8f8676 roughness 0.92 + ao:wall`；
   门把手 roughness 0.4→0.55、metalness 0.8→0.7。修复后 tp=5 白占比 0.0056%。
   注意：tp=50 复拍白占比随荧光灯闪烁状态波动（暗态 0.55% / 亮态 0.85%），亮态白簇落在门板纹理热斑边缘——
   属近距离手电筒物理过曝，非材质 bug（待 `?noflash` A/B 最终确认）。
4. **README 同步**：手电筒描述、570 网格/43 点光源实测值、阴影完全关闭说明、触控板提示、调试参数表。

---

## 2.7 第 4 轮修复（本轮）：穿地物理 bug（用户反馈「穿墙/穿地」的直接根因之一）

- **现象**：冒烟测试「player back on entry floor (y=0)」失败；玩家从西楼梯下到入口后 y=0.068
  悬空，结算循环以 dy=-0.5 大步下落时直接穿透地板掉出世界。
- **根因**（`js/util.js` `moveWithCollisions`）：dy<0 的落地窗口下界是 `foot - 0.001`。
  窗口锚定在「当前脚底」而非「本帧下落跨度」：角色悬浮 >1mm 时窗口不含地面，且单帧大步下落
  （传送掉落、结算循环）会跳过窗口 → 永远落不了地、穿地。
- **修复**：落地窗口改为 `[fallBottom - 0.001, foot + 0.101]`（本帧实际下落跨度）。
  接地角色不会下坠（自身支撑面恒在窗口内，低一级表面低于 fallBottom 被排除）；
  悬浮/大步下落角色立即落地。smoke 新增 2 项回归断言（大/小步下落均落地）。

## 2.8 第 4 轮修复：建模位置/朝向（用户反馈「建模没放在应有位置」）

全部在 `js/level.js`，几何审计（AABB 嵌入检测，见第 4.5 节）驱动修复：

| 项目 | 原问题 | 修复 |
| --- | --- | --- |
| 电视屏幕 + 鬼脸 | PlaneGeometry 法线 +z，朝向南墙，玩家只能看到背面 | `rotation.y = π`（朝向房间） |
| 卧室镜子 | x=-13.76，在墙（-13.9..-13.7）体内，不可见 | x=-13.695（卧室内壁 -13.7 前） |
| 浴室药柜 + 柜门 | 药柜中心 -13.9 半嵌墙；柜门 x=-13.98 整块在墙内 | 药柜 -13.93（凸出浴室面）、铰链门 -13.87 |
| 佛间照片 | 在 tier 东侧棱面朝 +z，玩家看到侧面；报纸 y=0.62 嵌在实心木板内 | 照片移到正面 x=7.145 朝 -x；报纸平放顶板 y=1.47 |
| 二楼北墙窗户 | face 'n' → 平面朝走廊方向（侧面对玩家）；'e' 窗台偏移方向反、埋在墙内 | face 改 'e'（法线 +x 入走廊）；窗台 +0.03（凸入房间） |
| 厨房水龙头 | 立管 z=7.45 嵌南墙（7.4..7.6） | z=7.31（水槽后缘），横管 7.21 |
| 浴缸水龙头/出水口 | y=1.25/1.42，缸沿才 0.62，悬空 0.6m | y=0.80/0.97（立在缸沿上） |
| 三份手记/报纸 | 悬浮台面 7cm 或嵌木板内 | 贴台面（0.56/0.73/1.47） |
| 抬高地段血迹贴花 | y=0.012 埋在 0.16m 地板下 | `decalFloor` 加 y 参数，两处改 0.172 |
| 浴室血污/手印 | x=-13.685 face 'w'：嵌在墙体（卧室侧）内，两面不可见 | x=-13.93 face 'e'（浴室侧墙面） |
| 暖气片 ×2 | body 中心 ±1.10，半嵌墙 0.05 | body ±1.075（凸出墙面） |
| 楼梯扶手 ×2 | ±1.115，整根在墙内（inter=1.00） | ±1.083（内壁 ±1.1 前） |
| 衣柜背板/侧板 | 下段嵌通道南墙 0.1；上段偏移 0.01 | 删除下段（南墙即端墙）；上段 13.86；地板 z 13.8..14.7 |
| 踢脚线 jog 段 | x=0.965 整根在墙内（墙内壁 0.95） | x=0.935 |
| 冰箱门线 | 水平板中心=冰箱中心，整块在冰箱体内 | 竖直门缝线贴前脸 z=3.29 |
| 书架上的书 | x=-8.26 在实心书柜箱体内，不可见 | x=-8.10（柜面外）；倒书 -8.09 |
| 灶台后排火眼 | z=7.45 嵌南墙 | z=7.29 |
| 佛坛蜡烛 ×2 | yTop=1.42 嵌坛面（1.35..1.45） | yTop=1.59（站在坛面上） |
| 热水器 | x=-13.86 整块在墙内 | x=-13.94（凸出浴室面） |
| 客厅挂画+画框 | z=7.51/7.53 在墙体内（客厅内壁 7.6） | z=7.615/7.63 |
| 儿童房柜子+门板 | 柜子中心 8.05 嵌东墙 0.075；门板为嵌体内水平板 | 柜子 7.95（贴墙留隙）；门板竖直贴前脸 |
| 管道卡扣 | 0.08 宽支架伸进二楼墙 | 0.04 宽贴管道 |

## 2.9 第 4 轮修复：怪物（用户反馈「不符合现实逻辑/穿墙」）

- **追击速度 2.35 → 3.3**：原速度比玩家步行（2.7）还慢，永远追不上；
  现在略慢于冲刺（3.9），必须跑起来才能逃生。
- **`_teleportTowardPlayer`（攻击冲刺）防穿墙**：从 0.55m 起逐档退到 0.8/1.1/1.5m 找空位，
  全堵则原地不动——玩家贴墙时怪物不再穿墙贴脸。
- **`_teleportNear` 过滤会嵌入墙体的节点**。

## 2.10 第 4 轮修复：电视

- 屏幕朝向修复（见 2.8）；**关机黑屏**：`tv.screen.visible = tv.on`（原来关机也常亮雪花，
  夜间是一块自发光白斑）。

## 2.11 第 4 轮修复：`?tp` 调试参数支持二楼

原实现固定从 y=0.9 掉落：二楼（地板 2.8）会直接穿过上层地板掉出世界；
低天花板处会穿天花板。现按脚下最高地板（<6m）+ 头顶最低天花板计算 dropY，
tp=62.3（二楼 landing）等点位可正确落地。仅影响调试参数。

## 2.12 第 4 轮修复：交互与事件逻辑

- **调查射线加墙体遮挡检测**（`_losBlocked`，预计算静态 collider 的 Box3）：
  不能隔着墙按 E 开门/读手记（原 raycast 只测 interactable mesh，墙不挡）。
- **随机关门事件**只关玩家 3m 以外的门——不再可能把玩家卡进正在关闭的门板里。
- **镜子惊吓**：鬼魂生成点从 1.7m 起逐档后退找空位，不再生成在墙里。
- **手电筒位置**改为沿视线方向 0.12m（原来固定 +0.1x，贴墙时偏移怪异）。

## 3. 遗留任务 / 待办（交给下一个 agent）

### 3.1 必须完成
1. ✅ **（第 3 轮已完成）flashboost 移除**：`_updatePlayer` 现为 `this.flash.intensity = this.flashOn ? 20 : 0;`；boot 段的 flashboost/drive/wall/nolights/noflash 五个参数一并删除，grep 验证无残留引用。
2. **确认本轮手电筒修复在真实 GPU 上有效**：SwiftShader 下已验证（A/B 差异 0.7→24.2），但真实 Mac GPU 行为应以用户实测为准。若真实 GPU 上 camera-attached spotlight 本来就能工作，则此修复是纯增强；若用户仍反馈手电筒问题，需继续排查（先看浏览器 console 有无 shader 报错）。
3. ✅ **（第 3 轮已完成）README 更新**：手电筒描述改为「体积光锥 + 每帧跟随相机照射」；性能一节改为实测 570 网格 + 43 点光源、完全关闭阴影贴图；操作表下新增触控板拖动提示；新增「回归/调试工具」一节文档化剩余 URL 参数。
4. ✅ **（第 3 轮已完成）调试代码清理**：保留 `?autostart/?nopost/?tp/?pos`（回归工具，已写入 README），删除其余五个。HANDOVER.md 保留作为方法论文档。
5. **（第 3 轮新增备忘）`renderer.shadowMap.enabled` 仍为 true 但全场景无 castShadow 光源**：three.js 不会为此执行 shadow pass（零开销），属语义死配置；若未来给某灯光开 castShadow，需先确认该开关仍为 true。

### 3.2 用户尚未反馈/需要主动回归验证的项
- **白色穿模物**：本轮拆掉了 6 个候选根因（过曝/光锥/阴影痤疮/暖气片/挂钟/错位道具）。用户尚未确认是否解决。**必须让用户实际游玩确认**，或在其机器上截图验证。
- **触控板灵敏度**：已从 0.0035 提到 0.006（拖动）和 0.0038（锁定）。若用户仍觉得慢，可继续上调（0.008-0.01 拖动力度），或做成可调灵敏度。
- **整体亮度平衡**：手电筒修复后场景整体变亮（固定光源 + 工作手电筒）。若用户觉得氛围变淡，可适度下调荧光灯强度（当前走廊 12、房间 7-9）或上调泛光阈值。
- **性能**：本轮新增网格较多（踢脚线、肋骨、书架书、玩具等），SwiftShader 下帧率已很低；真实 GPU 上应无压力，但可做一次 draw call 统计。

### 3.3 已知未修/潜在问题（供排查方向）
- ~~**`drive` 模式下虚拟时间 RAF 帧数极少**~~ → 该模式已在第 3 轮删除。教训保留：虚拟时间下 RAF 帧数不可靠，走位逻辑验证用 `smoke.mjs` 的 38 航点（已通过），视觉走位用 `?tp=` 传送定点截图。
- **幽灵少女 `appearAt` 的 y 参数**：部分 spawn 点 y 缺省 0，二楼 spawn（`{y:2.8}`）在 randomEvent 中 `s.y ?? 0` 正确。
- **`_randomEvent` 中的 silhouette 概率 0.18 附加在任何事件上**，可能与主事件同时触发字幕/音效堆叠——体验上可接受，但可考虑限制。
- **`level.update` 的滴水检测**仅在 `playerPos` 传入时工作（game 传了 camera.position，smoke 没传）——正常。
- **`?nosnap` 只在模块加载时读取一次**——刷新页面才生效。
- ~~**墙上的 ofuda 白纸条/涂鸦海报**~~ 已确认 ofuda 材质为 stdMat（受光），第 3 轮 7 点截图未见其过曝。
- **（第 3 轮）tp=50 死门角落残余白占比 0.15%（关灯）/0.55-0.85%（开灯）**：已定位为死门上方荧光灯（z=49.1）
  在门板嵌条高光 + 手电筒热斑边缘叠加的物理过曝，非材质 bug（?noflash A/B 确认关灯仍有 0.15% 但量级无害）。
  若用户专门抱怨这个角落，可把该灯 base 从 12 降到 9-10，或提高 toneMappingExposure 余量——先让用户实测再说。

---

## 4. 验证方法论（复制即用）

### 4.1 无头截图（本机路径）
```bash
CHROME="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"
# 每次运行必须用独立 user-data-dir！否则 Chrome 单例锁导致互相阻塞/挂死
"$CHROME" --headless=new --user-data-dir=/tmp/echo_prof_N \
  --use-angle=swiftshader --enable-unsafe-swiftshader \
  --disable-renderer-backgrounding --disable-background-timer-throttling \
  --disable-frame-rate-limit --run-all-compositor-stages-before-draw \
  --hide-scrollbars --window-size=640,360 --screenshot=out.png \
  --virtual-time-budget=3500 --timeout=20000 \
  "http://127.0.0.1:8010/?autostart=1&tp=16.8&face=s"
```
**铁律**：
- **永远不要并发多个 headless Chrome**（SwiftShader 互相抢 CPU，会挂 40+ 分钟）；一次一个，串行。
- **每次运行必须用独立 user-data-dir + URL 加 cache-buster（&cb=$RANDOM）**！python http.server 不发
  Cache-Control，Chrome 对"刚修改的文件"走启发式缓存——第 5 轮曾因此连续 4 批截图跑旧 JS，
  白斑调参全部无效（数值三位小数不变即中招）。两样都要：独立 profile 只防单例锁，防不住 HTTP 缓存。
- 加 `--timeout=20000` 防止无限挂起；必要时 `pkill -f "headless=new"`。
- **（第 3 轮新增）Chrome 写完截图后可能在退出阶段挂死**（--timeout 也救不了），管道/脚本会一直等。必须用看门狗：后台跑 Chrome，轮询截图文件出现（≤45s），出现即 `kill -9` 主进程 + `pkill -9 -P` 子进程，再进下一张。macOS 无 `timeout` 命令，别依赖它。
- 截图分析用 PIL：平均亮度、`min(p)>235` 白色占比、连通域聚类（>200 亮度团块定位）、孤立亮点计数（阴影痤疮特征）。
- **（第 3 轮新增）截图前先确认服务器真的在提供游戏**：`curl -s http://127.0.0.1:8010/ | head -3` 必须返回 `<!DOCTYPE html>` 而不是目录列表；服务器进程可能在会话间死掉（本轮就发生了），此时 headless Chrome 会截到 ERR_CONNECTION_REFUSED 白页（特征：所有 URL 截图字节级相同、mean≈250、title=IP）。服务器启动：`cd echo-apartment && nohup python3 -m http.server 8010 &`。

### 4.2 冒烟测试
```bash
cd echo-apartment && node smoke.mjs   # 36 项断言（第 4 轮 +2：穿地回归），全部通过为成功
```

### 4.3 语法检查
```bash
for f in js/*.js; do node --input-type=module --check < $f; done
```

### 4.4 关键数值速查（第 5 轮最终值）
- 渲染：RENDER_W=640, RENDER_H=360；EYE=1.55；PLAYER_H=1.75；PLAYER_R=0.3
- 色调映射：ACES，toneMappingExposure **1.38**（第 8 轮起 ACES+sRGB 在 grade shader 内完成，
  无 OutputPass；曝光经 uExposure uniform 注入。**铁律：抖动/颗粒必须在显示空间做**——
  线性空间 32 级量化的第一级≈21% 显示亮度，bayer 抖动在近黑区变成 0↔50 棋盘噪声）；
  半球环境光 **1.26**；雾 FogExp2 **0.062**
  （雾是唯一"只压远处"的杠杆：远处多灯累积亮度带靠它压回 235 以下；曝光在泛光之后应用，
  加曝光不会重新触发泛光白斑；环境光是纯漫反射永不产生热点）
- 胶片颗粒：显示空间幅度 **0.045 ×（0.3+0.7·smoothstep(0.05,0.25,lum)）**（第 8 轮；
  旧值线性空间 0.055 在暗部经 sRGB 斜率放大 3-4 倍——「暗场颗粒偏重」的第二根因）
- 贴图：magFilter=Nearest（近处像素块），minFilter=**LinearMipmapLinear + anisotropy 8**（消摩尔纹）；
  TV 雪花仍为流式 Nearest（每帧重生成，mipmap 无意义）
- 手电筒：强度 **5.5**，距离 18，angle 0.34，decay 1.7，penumbra 0.85，castShadow=false，目标 12m 前；
  位置沿视线 0.12m。1.1m 近墙热点 ~211（可读不过曝），3.6m 光池 ~215
- 光锥 mesh：半径 0.6，长 6.5，uOpacity **0.07**，**近相机 1.2m 内 smoothstep 淡出**
  （从锥内看，近眼段曾在屏幕中央叠加 0.1 亮度，把中等亮度墙面顶成实心白矩形）
- 泛光（grade shader 内建，作用于 HalfFloat 场景值、色调映射之前）：阈值 gt=**1.2**，uGlow 0.35
- 灰尘粒子：size 0.02，opacity 0.18，**相机 1.3m 气泡内重生**（近眼粒子以 sizeAttenuation 渲染成
  数十像素的加色白团——「移动白色大块」的第一根因）
- 荧光灯（PointLight decay 1.8，光源位于灯槽下 y-0.05）：走廊 2.8 / 厨房·客厅 3.0 / 寝室·儿童房 2.6 /
  浴室 2.5 / 佛间 2.4 / 二楼 2.4 / 玄关 2.6；**灯管为 MeshBasicMaterial 固定亮度**（0xc9d2d6，
  熄灭换 0x1e2124 暗玻璃——不受灯光影响，杜绝自照明白带）
- 其他灯：月光 0.8（距墙面 0.6m）、蜡烛 1.8（dist 4）、电视 1.4 闪、出口灯 0.9、热水器 1.2 红脉冲
- **材质铁律：场景无环境贴图，metalness >0.3 的道具会渲染成黑块**（散热器/管道/锅/门把手全中过招）。
  第 5 轮把 16 处 metalness 统一压到 ≤0.3 并放宽 roughness
- `?tp` 出生高度 +0.45m（原 +0.9m：下落中眼睛正好穿过天花灯具层 y 2.45-2.52，距点光源 6cm 处
  全屏炸白——曾是"厨房随机白斑"的真正本体，SwiftShader 帧数波动决定是否拍到）
- 移动速度 2.7 / 冲刺 3.9；怪物追击 3.3 / 潜行 0.9-1.5；stepUp 0.35；重力 22
- 音频：master 0.85 → 压缩器；卷积混响 1.9s 暗色 IR（send 0.35/voice）；BGM 动机间隔 9-16s-fear×6；
  追击层八分音脉冲 0.21s + 每 1.68s 不协和簇；雷声延迟 400+dist×3000ms；夜风 0.05×level

### 4.5 几何嵌入审计（第 5 轮已工具化，**常驻保留**）
`node debug-audit.mjs` 三项检查：
1. **mesh 嵌入审计**：全场景 mesh 世界 AABB 与静态 collider 求交，按相交体积比排序
   （剔除与自身 collider 重合的结构件）。剩余高分项多为「道具 vs 自身碰撞箱」「门框跨墙」「贴花贴墙面」等合理项——
   需要人工甄别，但**贴花 y 尺寸 > 0.05 的条目 = 贴花立起来了**（第 5 轮就是这么发现 decalFloor rotation.z bug 的）。
2. **穿墙压力测试**：对每个低层 collider 从 8 个方向以冲刺速度推玩家 90 步，统计穿透 > 5cm 的次数。
   已知假阳性：静态 collider 互相重叠处（东楼梯顶端台阶与端墙重叠 0.1m）、楼梯井下方封闭腔体（出生点在腔内）。
   真实穿透应为 0。
3. **怪物节点地板检查**：每个 monsterNodes 必须站在实际地板上
   （第 5 轮修复 4 个：抬高地段 y、楼梯内部、楼梯井悬空）。

### 4.6 白斑根因链（第 5 轮完整定位，共七层，全部修复）
「墙面大片白色」不是单一 bug，是一个七层叠加的洋葱。判别方法：`?nopost=1` 排除后期、`?noflash=1`
排除手电筒、同点位连拍 3 张看随机性（随机 = 粒子/下落/闪烁时序）。
1. **相机近旁灰尘粒子**（最隐蔽、动态）：sizeAttenuation 使距眼几厘米的粒子渲染成数十像素加色白团，
   随上飘移动。修复：粒子在相机 1.3m 气泡内重生 + size 0.02/opacity 0.18。
2. **远处荧光灯管自照明**：点光源离灯管仅 0.19m，受光材质的灯管在自己光照下到 255，远看是贯穿走廊
   的白带。修复：灯管改 MeshBasicMaterial 固定亮度。
3. **泛光阈值过低**：泛光作用于 HalfFloat 场景值，远处亮度带 ~1.1 越过 0.92 阈值被长成宽白斑。
   修复：gt=1.2。
4. **手电筒体积光锥近眼段**：从锥内看近眼段在屏幕中央叠加 0.1 亮度，把 ~220 的墙面顶成实心白矩形。
   修复：近相机 1.2m smoothstep 淡出 + uOpacity 0.07。
5. **墙纸"破损石膏条"亮带**：贴图里 #9a978a 亮条每 2.6m 重复，房间灯下整条过曝。修复：改暗于墙纸底色。
6. **?tp 下落穿灯**（仅调试路径）：+0.9m 出生下落时眼睛高度正好扫过天花灯具层。修复：+0.45m。
7. **远处多灯累积带 + 台面/瓷砖掠射高光**：雾 0.062 压远处；metalness>0.3 无 envmap 黑块与
   台面掠射白线成对出现（金属黑 vs 掠射白是同一次材质错误的两个方向）。修复：metalness≤0.3 统一。
最终验收：厨房×2/走廊/散热器近墙/卧室/二楼/抬高地段七点位 white% 全部 ≤0.03%，maxblob ≤48px。

### 4.7 摩尔纹度量
带后期（胶片颗粒 0.055）会淹没高频指标——**摩尔纹对比必须用 `?nopost=1` 截图**，
拉普拉斯能量集中在画面下 1/3（地板掠射角区域）。`/tmp/echo_analyze.py` 输出 hf-moire 列。
项目无 git——改贴图前若需 A/B 基线，先备份截图。

---

## 5. 关键文件与代码位置（js/ 均为 ES Module）

| 文件 | 职责 | 第 5 轮改动点 |
| --- | --- | --- |
| `js/game.js` | 主循环/玩家/事件/惊吓 | 曝光 1.3 + 半球光 1.05（存 this.hemi 供闪电用）；雾 0.048；闪电导演 `_updateLightning`；`_toggleSwitch`（开关交互+灯自己亮回惊吓）；`_floorSurface` 材质脚步声；blackout/lightsOut 改用 f.kill（灯管同步熄灭）；TV 灯 1.4 闪；`updateMusic`/`setWind` 接入主循环；儿童房 lullaby |
| `js/level.js` | 关卡/道具/灯光 | 灯具重做（灯槽+端盖+灯管双材质，make() 加 len 参数）；全部灯强度重平衡（见 4.4）；flicker 状态机（f.rng 持久随机流）；死门眼睛墙朝向 'e'；decalFloor rotation.z→y（贴花立起 bug）；怪物节点 4 处修正；楼梯扶手参数化重做+立柱；`_buildDetailProps`（瓷砖墙裙避开暗道缺口/信箱/伞架/沙发坐垫/被褥/防溅板+挂锅/儿童桌椅/浴帘杆）；窗户四边框+中梃；可交互开关（_buildLights 末尾）；落地灯双灯罩材质 |
| `js/textures.js` | 38 张贴图 | toTexture 改 mipmap+各向异性 8（消摩尔纹）；新增 toStreamingTexture（TV 雪花）；tileTex 浴室瓷砖；mailboxTex 信箱（3 号室模糊铭牌） |
| `js/audio.js` | 程序化音效引擎 | 程序化卷积混响（_buildReverb + _out send）；夜风循环 setWind；`updateMusic` 生成式配乐（动机层+追击层）；thunder/switchClick/buzz/lullaby；材质脚步声 footstep('wood'/'tatami'/'concrete') |
| `js/monster.js` | 怪物+幽灵 | 本轮未改（第 4 轮已修速度/防穿墙） |
| `js/util.js` | RNG/AABB 碰撞/PS1 抖动 | 本轮未改 |
| `index.html` | UI | 本轮未改 |
| `smoke.mjs` | 无头冒烟测试 | 未改，全过 |
| `debug-audit.mjs` | 几何/穿墙审计 | **新增且常驻**（嵌入审计+89k 步压力测试+节点地板检查，见 4.5） |
| `HANDOVER.md` | 本报告 | 第 5 轮更新 |

---

## 6. 给下一个 agent 的执行建议（按优先级）

1. **先让用户实测**：①墙面白斑（近距手电筒热点+远处灯带）是否消失/可接受；②摩尔纹（地板掠射角）是否还有闪烁；③新 BGM/混响/雷声/开关/闪电的体验；④整体亮度（走廊灯 2.8+曝光 1.3+雾 0.048）——太暗升曝光 1.35-1.4，太亮降回 1.25。
2. 若远处灯带仍是白线：降走廊灯（2.8→2.5）或升雾（0.048→0.055），不要动曝光（影响全图）。
3. **真实 GPU 专项**：SwiftShader 静态帧白像素≈0 只证明数值安全；真实 GPU 镜面高光更锐利——若再报"移动白斑"，查材质 roughness（本轮已把木地板 0.42→0.72）。
4. 每次改动后回归：`node smoke.mjs` + `node debug-audit.mjs`（真实穿透应为 0）+ 2-3 张 tp 截图（white% <0.3% 且 maxblob <100px，灯管小亮斑除外）。
5. 收尾惯例：杀测试服务器、清 /tmp/echo_prof_*；debug-audit.mjs 保留在仓库。

> 视觉模型（analyze_image MCP）持续 429 限流——截图判读用 PIL 数值指标（/tmp/echo_analyze.py：mean/white%/maxblob/hf-moire），必要时把可疑区域像素 dump 出来人工推理（本轮白线就是这么定位为"远处灯带过曝"的）。
